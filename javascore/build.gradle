dependencies {
    implementation 'foundation.icon:javaee-api:0.9.0'
}

sourceSets {
    main.java.srcDirs = [
        'samples/src',
    ]
}

compileJava {
    options.compilerArgs += ['-parameters']
}
